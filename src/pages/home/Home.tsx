import SearchBar from '../../widgets/searchBar/SearchBar';
import CardList from '../../widgets/cardList/CardList';

const Home = () => {
  return (
    <main className="main">
      <SearchBar />
      <CardList />
    </main>
  );
};

export default Home;
